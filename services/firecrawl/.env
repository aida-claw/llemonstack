# Firecrawl per-service env — intentionally minimal.
# All runtime configuration now lives in docker-compose.yaml environment: blocks,
# with secrets sourced from the stack llemonstack/.env (FIRECRAWL_* namespaced).
# The prior generation's vars (shared-redis URL, worker counts, PORT/HOST) were
# removed — they conflicted with the isolated/pinned 2.10.6 rewrite.
